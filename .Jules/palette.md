## 2025-02-28 - Add disabled states to missing inputs
**Learning:** Preventing users from clicking buttons when inputs are missing is a foundational UX/a11y check that prevents confusion and error states. This is especially true for inputs tied to empty UI feedback elements like list displays.
**Action:** Add disabled attributes connected to corresponding variables and add grayscale and opacity pseudo-class css rules to improve button disabled visualization.
